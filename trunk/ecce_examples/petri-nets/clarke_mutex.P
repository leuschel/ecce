/* ---------------- DESCRIPTION FILE ------------------------- *//* CLARKE MUTEX Example */trans(action,S,NewState) :- trans(S,NewState).trans(0,1). trans(0,2).trans(1,3). trans(1,4).trans(2,5). trans(2,6).trans(3,0). trans(3,7).trans(4,7). trans(5,8).trans(6,0). trans(6,8).trans(7,2). trans(8,1).prop(0,n1). prop(0,n2).prop(1,t1). prop(1,n2).prop(2,n1). prop(2,t2).prop(3,c1). prop(3,n2).prop(4,t1). prop(4,t2).prop(5,t1). prop(5,t2).prop(6,n1). prop(6,c2).prop(7,c1). prop(7,t2).prop(8,t1). prop(8,c2)./* The start state for the animator and for backwards analysis */start(0)./* Finite state model checking tasks */finite_state_model_check(clark_mutex_liveness,Formula) :-      Formula = sat(0,af(or(p(t1),p(t2))))./* Parametric model checking tasks */k_processes_model_check(_,_) :- fail.     /* Infinite Model Checking Tasks */infinite_model_check(_,_) :-     fail.