%% CIAO syntax
:- use_package([assertions]).  

:- comment(title,"Gathering all modules in a directory").

:- comment(author,"D. Cabeza, M. Carro").

:- comment(module,"A simple bash script for @concept{collecting files
   which are modules}, in order to compile automatically the CIAO libraries.

   @section{Usage (collect_modules)}

   @begin{verbatim}
   Usage: collect_modules module_name
   @end{verbatim}

   @section{More details (collect_modules)}

   Look at the Makefiles in lib and library

").


main.
