
:- module(tester_alt,[current_host/1],[assertions,isomodes,regtypes]).
%% :- module(tester_alt,[current_host/1],[]).

%% :- comment(doinclude,current_host/1).

%% :- use_module(tester_aux2).

:- true pred current_host(?atm).
% :- true pred current_host(?atm) + iso.

current_host(foo).
%% current_host(bar).
